{
  "name": "@papercusp/desktop-ipc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Endpoint-stream transport for Tauri desktop webviews: an IPC transport (over the Tauri bridge) + HTTP transport + a runtime picker, with the desktop-bootstrap fetch/EventSource polyfills. Routes /api/* over IPC to dodge the WebKitGTK/libsoup 6-connection-per-host limit.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./desktop-bootstrap": "./src/desktop-bootstrap.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@papercusp/tooldef": "*",
    "@papercusp/sse": "*",
    "@tauri-apps/api": "*"
  },
  "devDependencies": {
    "jsdom": "*",
    "vitest": "^4.1.4"
  }
}
